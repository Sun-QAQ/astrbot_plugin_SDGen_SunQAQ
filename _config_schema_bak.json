{
    "webui_url": {
        "type": "string",
        "description": "Stable Diffusion WebUI地址",
        "default": "http://127.0.0.1:7860",
        "hint": "http://IP:port或https://IP:port"
    },
    "negative_prompt": {
        "type": "string",
        "description": "固定负面提示词",
        "default": "(worst quality, low quality:1.4), deformed, bad anatomy",
        "hint": "自动附加到所有生成的负面提示词"
    },
    "default_steps": {
        "type": "int",
        "description": "默认迭代步数",
        "default": 20,
        "options": [15, 20, 25, 30]
    }
}
